# 今天做了什么

凌晨阶段冒烟测试仍在排错，后续白天陆续解决了 OpenCLIP 权重、VGG16 下载、显存 OOM、checkpoint 保存时磁盘空间不足等问题，最终将 batch_size 调到 2 并成功完成 50-step smoke test。之后完成 500-step round1 微调，跑了 fixed validation 和 case1~5 的 PSNR、SSIM、MSE 对比，发现数值指标持续提升，但 ckpt500 存在明显过平滑、锐度下降的问题。晚上又使用未训练的官方 HYPIR baseline 跑完主办方 100 张测试集，并按 output_dir/caseX.jpg 的要求整理和打包提交结果。
